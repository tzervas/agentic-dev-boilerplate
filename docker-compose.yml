version: '3.8'

services:
  test-agentic-boilerplate:
    build: .
    container_name: agentic-boilerplate-test
    volumes:
      - .:/app
      - /tmp/test-results:/tmp/test-results
    command: sh -c "echo 'Tests completed'"
